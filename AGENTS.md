# Agent Instructions

Read `README.md` for the public paper summary, then inspect `theory/latest/THEORY.tex` before making any mathematical claim about the staged theory.

For wiki ingestion, read `wiki/README.md` and use `wiki/latest/` as the current public vault. Treat `wiki/latest/wiki/graph.json` and `wiki/latest/wiki/nodes/` as structured provenance, not as a substitute for proof checking. Follow `parents`, backlinks, and node statuses before promoting any claim.

Use rendered Markdown LaTeX delimiters in public docs: `\(...\)` for inline math and `\[...\]` for display math. Keep formula-heavy statements out of Markdown tables when possible.

Do not infer private contact data from public review notes. Contact ledgers, raw logs, Oracle transcripts, and Gmail payloads are intentionally not published.

The latest staged theory snapshot is `stage_v034`; the public wiki vault points at the matching wiki snapshot.
