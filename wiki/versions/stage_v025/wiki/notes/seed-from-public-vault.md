# Seed From Public Vault

The repo was cloned from `https://github.com/pudim-project/pudim-ai-demo-zetalaw.git` into the previously blank local directory.

The public vault has two relevant signals:

- `wiki/latest/GOAL.md` names Erdos #536 as the durable broad research program.
- The newest entries in `wiki/latest/STATUS.md` and `wiki/latest/STRATEGY.md` focus on the exceptional \(n=2\) Qi--Lim--Nantomah Open Problem 4 beta-window endpoint.

For this v2 start, the active open node is the newest locally audited frontier: the \(n=2\) endpoint certificate. The Erdos #536 material remains part of the public background graph but is not the first v2 execution target.

The public graph statuses are not imported verbatim into v2 Theory. V2 Theory keeps only statement nodes with status `true` or `open`; definitions, provenance, and route notes stay in the Wiki.
